name: chemblstp # Will be concatenated with the group_id to an ID
author: iwatobipen(Takayuki Serizawa)
env_yml_path: ../my_conda_env.yml # This is necessary for bundling, but not needed during development
extension_module: chemblsp # The .py Python module containing the nodes of your extension
description: chembl_structure_pipeline # Human readable bundle name / description
long_description: chembl structure pipeline for compound standardization
group_id: org.chemoinfo
version: 0.1.0 # Version of this Python node extension
vendor: KNIME AG, Zurich, Switzerland
license_file: LICENSE.TXT # Best practice: put your LICENSE.TXT next to the knime.yml; otherwise you would need to change to path/to/LICENSE.txt
feature_dependencies:
  - org.rdkit.knime.binaries.feature 4.7.0
